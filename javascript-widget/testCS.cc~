#include "cairoShim.h"
#include "HTMLCanvas.h"
#include <emscripten/bind.h>

using namespace ravel;

EMSCRIPTEN_BINDINGS(CairoShim) {
  class_<CairoShim<HTMLCanvas>>(CairoShim)
    .function("moveTo",&CairoShim<HTMLCanvas>::moveTo)
    .function("lineTo",&CairoShim<HTMLCanvas>::moveTo)
    ;
}
